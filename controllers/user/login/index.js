const nanoid = require("nanoid")
const userModel = require("../../../model/user")
module.exports = {
    loginUser: async (req,res)=>{
        try {
            
        } catch (error) {
            
        }
    }
}